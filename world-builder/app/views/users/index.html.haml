%h1 All Users

%table#users
  %thead
    %tr
      %th User Name
      %th Email

  %tbody
    - @users.each do |user|
      %tr
        %td= user.name
        %td= user.email
= link_to 'Back to campaign list', campaigns_path, :class => 'button'